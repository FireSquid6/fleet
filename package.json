{
  "name": "fleet-monorepo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test tests",
    "typecheck:tests": "tsc --noEmit -p tests/tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "webterm": "workspace:*",
    "git-bun": "workspace:*",
    "libghostty-bun": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
