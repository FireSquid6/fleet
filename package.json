{
  "name": "fleet-monorepo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun run --filter='*' test",
    "typecheck": "bun run --filter='*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "webterm": "workspace:*",
    "git-bun": "workspace:*",
    "bun-vt": "workspace:*",
    "fleet-protocol": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
